export class Title {
  romaji: string;
  english: string;
  native: string;

  constructor(romaji: string, english: string, native: string) {
    this.romaji = romaji;
    this.english = english;
    this.native = native;
  }
}
