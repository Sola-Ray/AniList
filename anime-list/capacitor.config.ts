import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'list.anime.animeList',
  appName: 'anime-list',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
